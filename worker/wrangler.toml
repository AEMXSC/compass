name = "compass-ims-proxy"
main = "ims-token-proxy.js"
compatibility_date = "2024-01-01"

# Deploy with: npx wrangler deploy
# After deployment, update IMS_TOKEN_PROXY in ims.js with the worker URL
# e.g. https://compass-ims-proxy.<your-account>.workers.dev
