name = "compass-proxy"
main = "compass-proxy.js"
compatibility_date = "2024-01-01"

# Deploy:  npx wrangler deploy --config wrangler-proxy.toml
# Then add compass.aemxsc.com as a custom domain in the Cloudflare dashboard
# Workers & Pages → compass-proxy → Settings → Domains & Routes → Add Custom Domain
